export const Hole =()=>{
    const rabbitHoleStyles = {
        position: 'absolute',
        left: '300px',
        top: '250px'
    };

    return (
        <span style={rabbitHoleStyles} role="img" aria-label="hole">
            🕳️
          
        </span>
    );
}
